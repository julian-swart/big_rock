head(participants)
str(participants)
# check state names
participants %>%
count(state, sort = T) %>%
print(n = 23)
# check boat brands
participants %>%
pull(boat_brand) %>%
unique() %>%
sort()
# fix states and boat brands
participants <-
participants %>%
mutate(state = case_when(
str_detect(string = state, pattern = "NC|North") == TRUE ~ "NC",
str_detect(string = state, pattern = "FL|Fl") == TRUE ~ "FL",
str_detect(string = state, pattern = "MD|Maryland") == TRUE ~ "MD",
str_detect(string = state, pattern = "VA") == TRUE ~ "VA")
) %>%
mutate(boat_brand = case_when(
str_detect(string = boat_brand, pattern = "BCBuddy Cannady|BC|Canady") == TRUE ~ "Buddy Cannady",
str_detect(string = boat_brand, pattern = "Grady White") == TRUE ~ "Grady White",
str_detect(string = boat_brand, pattern = "Cabo") == TRUE ~ "Cabo",
str_detect(string = boat_brand, pattern = "BuddyDavis") == TRUE ~ "Buddy Davis",
str_detect(string = boat_brand, pattern = "Custom Carolina") == TRUE ~ "Custom Carolina",
str_detect(string = boat_brand, pattern = "Ocean") == TRUE ~ "Ocean Yacht",
str_detect(string = boat_brand, pattern = "Winter") == TRUE ~ "Winter",
TRUE ~ boat_brand)
) %>%
mutate(city = ifelse(str_detect(string = city, pattern = "New BErn") == TRUE, "New Bern", city),
city = str_trim()
)
participants <-
participants_raw %>%
separate(type, into = c("boat_length", "boat_brand"), sep = "' ", remove = F
) %>%
separate(port, into = c("city", "state"), sep = ",", remove = F
) %>%
mutate(boat_length = as.integer(boat_length))
participants <-
participants %>%
mutate(state = case_when(
str_detect(string = state, pattern = "NC|North") == TRUE ~ "NC",
str_detect(string = state, pattern = "FL|Fl") == TRUE ~ "FL",
str_detect(string = state, pattern = "MD|Maryland") == TRUE ~ "MD",
str_detect(string = state, pattern = "VA") == TRUE ~ "VA")
) %>%
mutate(boat_brand = case_when(
str_detect(string = boat_brand, pattern = "BCBuddy Cannady|BC|Canady") == TRUE ~ "Buddy Cannady",
str_detect(string = boat_brand, pattern = "Grady White") == TRUE ~ "Grady White",
str_detect(string = boat_brand, pattern = "Cabo") == TRUE ~ "Cabo",
str_detect(string = boat_brand, pattern = "BuddyDavis") == TRUE ~ "Buddy Davis",
str_detect(string = boat_brand, pattern = "Custom Carolina") == TRUE ~ "Custom Carolina",
str_detect(string = boat_brand, pattern = "Ocean") == TRUE ~ "Ocean Yacht",
str_detect(string = boat_brand, pattern = "Winter") == TRUE ~ "Winter",
TRUE ~ boat_brand)
) %>%
mutate(city = ifelse(str_detect(string = city, pattern = "New BErn") == TRUE, "New Bern", city),
city = str_trim()
)
participants <-
participants %>%
mutate(state = case_when(
str_detect(string = state, pattern = "NC|North") == TRUE ~ "NC",
str_detect(string = state, pattern = "FL|Fl") == TRUE ~ "FL",
str_detect(string = state, pattern = "MD|Maryland") == TRUE ~ "MD",
str_detect(string = state, pattern = "VA") == TRUE ~ "VA")
) %>%
mutate(boat_brand = case_when(
str_detect(string = boat_brand, pattern = "BCBuddy Cannady|BC|Canady") == TRUE ~ "Buddy Cannady",
str_detect(string = boat_brand, pattern = "Grady White") == TRUE ~ "Grady White",
str_detect(string = boat_brand, pattern = "Cabo") == TRUE ~ "Cabo",
str_detect(string = boat_brand, pattern = "BuddyDavis") == TRUE ~ "Buddy Davis",
str_detect(string = boat_brand, pattern = "Custom Carolina") == TRUE ~ "Custom Carolina",
str_detect(string = boat_brand, pattern = "Ocean") == TRUE ~ "Ocean Yacht",
str_detect(string = boat_brand, pattern = "Winter") == TRUE ~ "Winter",
TRUE ~ boat_brand)
)
head(participants_raw)
participants <-
participants_raw %>%
separate(type, into = c("boat_length", "boat_brand"), sep = "' ", remove = F
) %>%
separate(port, into = c("city", "state"), sep = ",", remove = F
) %>%
mutate(boat_length = as.integer(boat_length))
head(participants)
str(participants)
# check state names
participants %>%
count(state, sort = T) %>%
print(n = 23)
# check boat brands
participants %>%
pull(boat_brand) %>%
unique() %>%
sort()
# fix states and boat brands
participants <-
participants %>%
mutate(state = case_when(
str_detect(string = state, pattern = "NC|North") == TRUE ~ "NC",
str_detect(string = state, pattern = "FL|Fl") == TRUE ~ "FL",
str_detect(string = state, pattern = "MD|Maryland") == TRUE ~ "MD",
str_detect(string = state, pattern = "VA") == TRUE ~ "VA")
) %>%
mutate(boat_brand = case_when(
str_detect(string = boat_brand, pattern = "BCBuddy Cannady|BC|Canady") == TRUE ~ "Buddy Cannady",
str_detect(string = boat_brand, pattern = "Grady White") == TRUE ~ "Grady White",
str_detect(string = boat_brand, pattern = "Cabo") == TRUE ~ "Cabo",
str_detect(string = boat_brand, pattern = "BuddyDavis") == TRUE ~ "Buddy Davis",
str_detect(string = boat_brand, pattern = "Custom Carolina") == TRUE ~ "Custom Carolina",
str_detect(string = boat_brand, pattern = "Ocean") == TRUE ~ "Ocean Yacht",
str_detect(string = boat_brand, pattern = "Winter") == TRUE ~ "Winter",
TRUE ~ boat_brand)
) %>%
mutate(city = case_when(
str_detect(string = city, pattern = "New BErn") == TRUE ~ "New Bern",
str_detect(string = city, pattern = "") == TRUE ~ NA,
TRUE ~ city)
)
participants <-
participants_raw %>%
separate(type, into = c("boat_length", "boat_brand"), sep = "' ", remove = F
) %>%
separate(port, into = c("city", "state"), sep = ",", remove = F
) %>%
mutate(boat_length = as.integer(boat_length))
participants <-
participants %>%
mutate(state = case_when(
str_detect(string = state, pattern = "NC|North") == TRUE ~ "NC",
str_detect(string = state, pattern = "FL|Fl") == TRUE ~ "FL",
str_detect(string = state, pattern = "MD|Maryland") == TRUE ~ "MD",
str_detect(string = state, pattern = "VA") == TRUE ~ "VA")
) %>%
mutate(boat_brand = case_when(
str_detect(string = boat_brand, pattern = "BCBuddy Cannady|BC|Canady") == TRUE ~ "Buddy Cannady",
str_detect(string = boat_brand, pattern = "Grady White") == TRUE ~ "Grady White",
str_detect(string = boat_brand, pattern = "Cabo") == TRUE ~ "Cabo",
str_detect(string = boat_brand, pattern = "BuddyDavis") == TRUE ~ "Buddy Davis",
str_detect(string = boat_brand, pattern = "Custom Carolina") == TRUE ~ "Custom Carolina",
str_detect(string = boat_brand, pattern = "Ocean") == TRUE ~ "Ocean Yacht",
str_detect(string = boat_brand, pattern = "Winter") == TRUE ~ "Winter",
TRUE ~ boat_brand)
)
participants <-
participants %>%
mutate(state = case_when(
str_detect(string = state, pattern = "NC|North") == TRUE ~ "NC",
str_detect(string = state, pattern = "FL|Fl") == TRUE ~ "FL",
str_detect(string = state, pattern = "MD|Maryland") == TRUE ~ "MD",
str_detect(string = state, pattern = "VA") == TRUE ~ "VA")
) %>%
mutate(boat_brand = case_when(
str_detect(string = boat_brand, pattern = "BCBuddy Cannady|BC|Canady") == TRUE ~ "Buddy Cannady",
str_detect(string = boat_brand, pattern = "Grady White") == TRUE ~ "Grady White",
str_detect(string = boat_brand, pattern = "Cabo") == TRUE ~ "Cabo",
str_detect(string = boat_brand, pattern = "BuddyDavis") == TRUE ~ "Buddy Davis",
str_detect(string = boat_brand, pattern = "Custom Carolina") == TRUE ~ "Custom Carolina",
str_detect(string = boat_brand, pattern = "Ocean") == TRUE ~ "Ocean Yacht",
str_detect(string = boat_brand, pattern = "Winter") == TRUE ~ "Winter",
TRUE ~ boat_brand)
) %>%
mutate(city = case_when(
str_detect(string = city, pattern = "New BErn") == TRUE ~ "New Bern",
str_detect(string = city, pattern = "") == TRUE ~ NA,
TRUE ~ city)
)
participants <-
participants %>%
mutate(state = case_when(
str_detect(string = state, pattern = "NC|North") == TRUE ~ "NC",
str_detect(string = state, pattern = "FL|Fl") == TRUE ~ "FL",
str_detect(string = state, pattern = "MD|Maryland") == TRUE ~ "MD",
str_detect(string = state, pattern = "VA") == TRUE ~ "VA")
) %>%
mutate(boat_brand = case_when(
str_detect(string = boat_brand, pattern = "BCBuddy Cannady|BC|Canady") == TRUE ~ "Buddy Cannady",
str_detect(string = boat_brand, pattern = "Grady White") == TRUE ~ "Grady White",
str_detect(string = boat_brand, pattern = "Cabo") == TRUE ~ "Cabo",
str_detect(string = boat_brand, pattern = "BuddyDavis") == TRUE ~ "Buddy Davis",
str_detect(string = boat_brand, pattern = "Custom Carolina") == TRUE ~ "Custom Carolina",
str_detect(string = boat_brand, pattern = "Ocean") == TRUE ~ "Ocean Yacht",
str_detect(string = boat_brand, pattern = "Winter") == TRUE ~ "Winter",
TRUE ~ boat_brand)
) %>%
mutate(city = case_when(
str_detect(string = city, pattern = "New BErn") == TRUE ~ "New Bern",
str_detect(string = city, pattern = "") == TRUE ~ 'NA',
TRUE ~ city)
)
participants <-
participants_raw %>%
separate(type, into = c("boat_length", "boat_brand"), sep = "' ", remove = F
) %>%
separate(port, into = c("city", "state"), sep = ",", remove = F
) %>%
mutate(boat_length = as.integer(boat_length))
participants <-
participants %>%
mutate(state = case_when(
str_detect(string = state, pattern = "NC|North") == TRUE ~ "NC",
str_detect(string = state, pattern = "FL|Fl") == TRUE ~ "FL",
str_detect(string = state, pattern = "MD|Maryland") == TRUE ~ "MD",
str_detect(string = state, pattern = "VA") == TRUE ~ "VA")
) %>%
mutate(boat_brand = case_when(
str_detect(string = boat_brand, pattern = "BCBuddy Cannady|BC|Canady") == TRUE ~ "Buddy Cannady",
str_detect(string = boat_brand, pattern = "Grady White") == TRUE ~ "Grady White",
str_detect(string = boat_brand, pattern = "Cabo") == TRUE ~ "Cabo",
str_detect(string = boat_brand, pattern = "BuddyDavis") == TRUE ~ "Buddy Davis",
str_detect(string = boat_brand, pattern = "Custom Carolina") == TRUE ~ "Custom Carolina",
str_detect(string = boat_brand, pattern = "Ocean") == TRUE ~ "Ocean Yacht",
str_detect(string = boat_brand, pattern = "Winter") == TRUE ~ "Winter",
TRUE ~ boat_brand)
) %>%
mutate(city = case_when(
str_detect(string = city, pattern = "New BErn") == TRUE ~ "New Bern",
TRUE ~ city),
city = ifelse(city == "", NA, city),
city = str_trim(city)
)
sort(unique(participants$city))
participants <-
participants %>%
mutate(state = case_when(
str_detect(string = state, pattern = "NC|North") == TRUE ~ "NC",
str_detect(string = state, pattern = "FL|Fl") == TRUE ~ "FL",
str_detect(string = state, pattern = "MD|Maryland") == TRUE ~ "MD",
str_detect(string = state, pattern = "VA") == TRUE ~ "VA")
) %>%
mutate(boat_brand = case_when(
str_detect(string = boat_brand, pattern = "BCBuddy Cannady|BC|Canady") == TRUE ~ "Buddy Cannady",
str_detect(string = boat_brand, pattern = "Grady White") == TRUE ~ "Grady White",
str_detect(string = boat_brand, pattern = "Cabo") == TRUE ~ "Cabo",
str_detect(string = boat_brand, pattern = "BuddyDavis") == TRUE ~ "Buddy Davis",
str_detect(string = boat_brand, pattern = "Custom Carolina") == TRUE ~ "Custom Carolina",
str_detect(string = boat_brand, pattern = "Ocean") == TRUE ~ "Ocean Yacht",
str_detect(string = boat_brand, pattern = "Winter") == TRUE ~ "Winter",
TRUE ~ boat_brand)
) %>%
mutate(city = case_when(
str_detect(string = city, pattern = "New BErn") == TRUE ~ "New Bern",
TRUE ~ city),
city = ifelse(city == "", NA, city),
city = str_trim(city)
) %>%
unite(col = "port", c("city", "state"), sep = ",")
participants <-
participants_raw %>%
separate(type, into = c("boat_length", "boat_brand"), sep = "' ", remove = F
) %>%
separate(port, into = c("city", "state"), sep = ",", remove = F
) %>%
mutate(boat_length = as.integer(boat_length))
head(participants)
str(participants)
# check state names
participants %>%
count(state, sort = T) %>%
print(n = 23)
# check boat brands
participants %>%
pull(boat_brand) %>%
unique() %>%
sort()
# fix states and boat brands
participants <-
participants %>%
mutate(state = case_when(
str_detect(string = state, pattern = "NC|North") == TRUE ~ "NC",
str_detect(string = state, pattern = "FL|Fl") == TRUE ~ "FL",
str_detect(string = state, pattern = "MD|Maryland") == TRUE ~ "MD",
str_detect(string = state, pattern = "VA") == TRUE ~ "VA")
) %>%
mutate(boat_brand = case_when(
str_detect(string = boat_brand, pattern = "BCBuddy Cannady|BC|Canady") == TRUE ~ "Buddy Cannady",
str_detect(string = boat_brand, pattern = "Grady White") == TRUE ~ "Grady White",
str_detect(string = boat_brand, pattern = "Cabo") == TRUE ~ "Cabo",
str_detect(string = boat_brand, pattern = "BuddyDavis") == TRUE ~ "Buddy Davis",
str_detect(string = boat_brand, pattern = "Custom Carolina") == TRUE ~ "Custom Carolina",
str_detect(string = boat_brand, pattern = "Ocean") == TRUE ~ "Ocean Yacht",
str_detect(string = boat_brand, pattern = "Winter") == TRUE ~ "Winter",
TRUE ~ boat_brand)
) %>%
mutate(city = case_when(
str_detect(string = city, pattern = "New BErn") == TRUE ~ "New Bern",
TRUE ~ city),
city = ifelse(city == "", NA, city),
city = str_trim(city)
) %>%
unite(col = "port", c("city", "state"), sep = ",", remove = FALSE)
participants <-
participants %>%
mutate(state = case_when(
str_detect(string = state, pattern = "NC|North") == TRUE ~ "NC",
str_detect(string = state, pattern = "FL|Fl") == TRUE ~ "FL",
str_detect(string = state, pattern = "MD|Maryland") == TRUE ~ "MD",
str_detect(string = state, pattern = "VA") == TRUE ~ "VA")
) %>%
mutate(boat_brand = case_when(
str_detect(string = boat_brand, pattern = "BCBuddy Cannady|BC|Canady") == TRUE ~ "Buddy Cannady",
str_detect(string = boat_brand, pattern = "Grady White") == TRUE ~ "Grady White",
str_detect(string = boat_brand, pattern = "Cabo") == TRUE ~ "Cabo",
str_detect(string = boat_brand, pattern = "BuddyDavis") == TRUE ~ "Buddy Davis",
str_detect(string = boat_brand, pattern = "Custom Carolina") == TRUE ~ "Custom Carolina",
str_detect(string = boat_brand, pattern = "Ocean") == TRUE ~ "Ocean Yacht",
str_detect(string = boat_brand, pattern = "Winter") == TRUE ~ "Winter",
TRUE ~ boat_brand)
) %>%
mutate(city = case_when(
str_detect(string = city, pattern = "New BErn") == TRUE ~ "New Bern",
TRUE ~ city),
city = ifelse(city == "", NA, city),
city = str_trim(city)
) %>%
unite(col = "port", c("city", "state"), sep = ",", remove = FALSE
) %>%
select(boat_name, type, boat_length, boat_brand, city, state, port, owner)
participants <-
participants_raw %>%
separate(type, into = c("boat_length", "boat_brand"), sep = "' ", remove = F
) %>%
separate(port, into = c("city", "state"), sep = ",", remove = F
) %>%
mutate(boat_length = as.integer(boat_length))
head(participants)
str(participants)
# check state names
participants %>%
count(state, sort = T) %>%
print(n = 23)
# check boat brands
participants %>%
pull(boat_brand) %>%
unique() %>%
sort()
# fix states and boat brands
participants <-
participants %>%
mutate(state = case_when(
str_detect(string = state, pattern = "NC|North") == TRUE ~ "NC",
str_detect(string = state, pattern = "FL|Fl") == TRUE ~ "FL",
str_detect(string = state, pattern = "MD|Maryland") == TRUE ~ "MD",
str_detect(string = state, pattern = "VA") == TRUE ~ "VA")
) %>%
mutate(boat_brand = case_when(
str_detect(string = boat_brand, pattern = "BCBuddy Cannady|BC|Canady") == TRUE ~ "Buddy Cannady",
str_detect(string = boat_brand, pattern = "Grady White") == TRUE ~ "Grady White",
str_detect(string = boat_brand, pattern = "Cabo") == TRUE ~ "Cabo",
str_detect(string = boat_brand, pattern = "BuddyDavis") == TRUE ~ "Buddy Davis",
str_detect(string = boat_brand, pattern = "Custom Carolina") == TRUE ~ "Custom Carolina",
str_detect(string = boat_brand, pattern = "Ocean") == TRUE ~ "Ocean Yacht",
str_detect(string = boat_brand, pattern = "Winter") == TRUE ~ "Winter",
TRUE ~ boat_brand)
) %>%
mutate(city = case_when(
str_detect(string = city, pattern = "New BErn") == TRUE ~ "New Bern",
TRUE ~ city),
city = ifelse(city == "", NA, city),
city = str_trim(city)
) %>%
unite(col = "port", c("city", "state"), sep = ",", remove = FALSE
) %>%
select(boat_name, type, boat_length, boat_brand, city, state, port, owner)
# tidy
# tidy participants data frame ----
head(participants_raw)
participants <-
participants_raw %>%
separate(type, into = c("boat_length", "boat_brand"), sep = "' ", remove = F
) %>%
separate(port, into = c("city", "state"), sep = ",", remove = F
) %>%
mutate(boat_length = as.integer(boat_length))
head(participants)
str(participants)
# check state names
participants %>%
count(state, sort = T) %>%
print(n = 23)
# check boat brands
participants %>%
pull(boat_brand) %>%
unique() %>%
sort()
# fix states and boat brands
participants <-
participants %>%
mutate(state = case_when(
str_detect(string = state, pattern = "NC|North") == TRUE ~ "NC",
str_detect(string = state, pattern = "FL|Fl") == TRUE ~ "FL",
str_detect(string = state, pattern = "MD|Maryland") == TRUE ~ "MD",
str_detect(string = state, pattern = "VA") == TRUE ~ "VA")
) %>%
mutate(boat_brand = case_when(
str_detect(string = boat_brand, pattern = "BCBuddy Cannady|BC|Canady") == TRUE ~ "Buddy Cannady",
str_detect(string = boat_brand, pattern = "Grady White") == TRUE ~ "Grady White",
str_detect(string = boat_brand, pattern = "Cabo") == TRUE ~ "Cabo",
str_detect(string = boat_brand, pattern = "BuddyDavis") == TRUE ~ "Buddy Davis",
str_detect(string = boat_brand, pattern = "Custom Carolina") == TRUE ~ "Custom Carolina",
str_detect(string = boat_brand, pattern = "Ocean") == TRUE ~ "Ocean Yacht",
str_detect(string = boat_brand, pattern = "Winter") == TRUE ~ "Winter",
TRUE ~ boat_brand)
) %>%
mutate(city = case_when(
str_detect(string = city, pattern = "New BErn") == TRUE ~ "New Bern",
TRUE ~ city),
city = ifelse(city == "", NA, city),
city = str_trim(city)
) %>%
unite(col = "port", c("city", "state"), sep = ", ", remove = FALSE
) %>%
select(boat_name, type, boat_length, boat_brand, city, state, port, owner)
library(zipcode)
install.packages("zipcode")
library(zipcode)
head(zipcode)
data("zipcode")
head(zipcode)
View(zipcode)
activity %>% select(port) %>% inner_join(zipcode %>% as.tibble() %>% select(city, state, latitude, longitude) %>% unite(col = 'port', c("city", "state"), sep = ", "))
participants %>% select(port) %>% inner_join(zipcode %>% as.tibble() %>% select(city, state, latitude, longitude) %>% unite(col = 'port', c("city", "state"), sep = ", "))
GNsearchAF <- function(x) {
res <- GNsearch(name=x, country="AF")
return(res[1, ])
}
zipcode <- NULL
library(ggmap)
install.packages("ggmap")
library(ggmap)
cbind(participants %>% select(port), geocode(port))
cbind(participants %>% select(port), geocode(participants$port))
?register_google
participants %>% select(port) %>% mutate_geocode(port)
install.packages("map")
install.packages("maps")
library(maps)
maps::map("state")
map.cities(x = participants %>% select(port))
map.cities(x = participants %>% select(city))
warnings()
data(world.cities)
View(world.cities)
map.cities(x = participants %>% select(city), country = "United States")
map.cities(x = participants %>% select(city), country = "USA")
warnings()
map.cities(x = participants %>% select(city), country = "USA")
participants %>% select(city)
participants %>% select(city) %>% filter(!is.na())
participants %>% select(city) %>% filter(!is.na(city))
map.cities(participants %>% select(city) %>% filter(!is.na(city)), country = "USA")
participants %>% select(city) %>% filter(!is.na(city)) %>% inner_join(world.cities, by = c("name" = "city"))
participants %>% select(city) %>% filter(!is.na(city)) %>% inner_join(world.cities, by = c("city" = "name"))
data("us.cities")
head(us.cities)
participants <-
participants_raw %>%
separate(type, into = c("boat_length", "boat_brand"), sep = "' ", remove = F
) %>%
separate(port, into = c("city", "state"), sep = ",", remove = F
) %>%
mutate(boat_length = as.integer(boat_length))
head(participants)
str(participants)
# check state names
participants %>%
count(state, sort = T) %>%
print(n = 23)
# check boat brands
participants %>%
pull(boat_brand) %>%
unique() %>%
sort()
# fix states and boat brands
participants <-
participants %>%
mutate(state = case_when(
str_detect(string = state, pattern = "NC|North") == TRUE ~ "NC",
str_detect(string = state, pattern = "FL|Fl") == TRUE ~ "FL",
str_detect(string = state, pattern = "MD|Maryland") == TRUE ~ "MD",
str_detect(string = state, pattern = "VA") == TRUE ~ "VA")
) %>%
mutate(boat_brand = case_when(
str_detect(string = boat_brand, pattern = "BCBuddy Cannady|BC|Canady") == TRUE ~ "Buddy Cannady",
str_detect(string = boat_brand, pattern = "Grady White") == TRUE ~ "Grady White",
str_detect(string = boat_brand, pattern = "Cabo") == TRUE ~ "Cabo",
str_detect(string = boat_brand, pattern = "BuddyDavis") == TRUE ~ "Buddy Davis",
str_detect(string = boat_brand, pattern = "Custom Carolina") == TRUE ~ "Custom Carolina",
str_detect(string = boat_brand, pattern = "Ocean") == TRUE ~ "Ocean Yacht",
str_detect(string = boat_brand, pattern = "Winter") == TRUE ~ "Winter",
TRUE ~ boat_brand)
) %>%
mutate(city = case_when(
str_detect(string = city, pattern = "New BErn") == TRUE ~ "New Bern",
TRUE ~ city),
city = ifelse(city == "", NA, city),
city = str_trim(city)
) %>%
unite(col = "port", c("city", "state"), sep = " ", remove = FALSE
) %>%
select(boat_name, type, boat_length, boat_brand, city, state, port, owner)
participants %>% select(port) %>% filter(!is.na(port)) %>% inner_join(us.cities, by = c("port" = "name"))
participants %>% select(port) %>% filter(!is.na(port)) %>% inner_join(us.cities, by = c("port" = "name")) %>% unique()
View(us.cities)
data(state)
head(state)
data(state)
head(state)
head(state.name)
